/**
 * Reserved provider stub. Gemini integration is prepared but not required yet.
 */
export class GeminiProvider {
  async generateResponse() {
    throw new Error('Provider "gemini" is reserved for future support.');
  }
}
