/**
 * Reserved provider stub. OpenAI integration is prepared but not required yet.
 */
export class OpenAIProvider {
  async generateResponse() {
    throw new Error('Provider "openai" is reserved for future support.');
  }
}
