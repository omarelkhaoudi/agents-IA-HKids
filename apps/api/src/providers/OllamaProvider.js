/**
 * Reserved provider stub. Ollama integration is prepared but not required yet.
 */
export class OllamaProvider {
  async generateResponse() {
    throw new Error('Provider "ollama" is reserved for future support.');
  }
}
